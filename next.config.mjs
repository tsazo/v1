/** @type {import('next').NextConfig} */
const nextConfig = {
    // images: {
    //     remotePatterns: {
    //         protocol: "https",
    //         hostname: "", // Main host url
    //     }
    // }
};

export default nextConfig;
